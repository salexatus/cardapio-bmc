---
import Base from '../layouts/Base.astro'
import SiteApp from '../apps/SiteApp.jsx'
---

<Base>
  <!-- Ilha React: site público. client:only evita mismatch de SSR com framer-motion. -->
  <SiteApp client:only="react">
    <div slot="fallback" class="grid min-h-screen place-items-center bg-sand-50 text-forest-600/50">
      Carregando cardápio…
    </div>
  </SiteApp>
</Base>
